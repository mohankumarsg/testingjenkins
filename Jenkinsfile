pipeline {
  agent any
  stages {
    stage ('Initialize') {
      steps ('run the script') { 
        script {
          echo "welcome to jenkins new pipelineSC"
          var1=mohan
          echo "hi welcome to ${var1}"
        }
      }
    }
  }
}
